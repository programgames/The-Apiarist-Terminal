# Access transformer (empty placeholder). Add entries if future access to private members is required.
# Must stay at META-INF/ocgendustry_at.cfg: the jar manifest declares FMLAT=ocgendustry_at.cfg,
# which FML resolves relative to META-INF.
